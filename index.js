// Generated by LiveScript 1.2.0
var x$;
x$ = angular.module('main', []);
x$.controller('main', ['$scope', '$http', '$sce'].concat(function($scope, $http, $sce){
  $scope.show = {
    0: true
  };
  $scope.goto = function(it){
    $scope.show[it] = true;
    return console.log(it, $scope.show);
  };
  return $http({
    url: '/sample.json',
    method: 'GET'
  }).success(function(d){
    return $scope.entries = d.feed.entry.map(function(it){
      var optionCount, i;
      optionCount = parseInt(it['gsx$num'].$t);
      return import$({}, {
        title: it['gsx$title'].$t,
        desc: $sce.trustAsHtml(markdown.toHTML(it['gsx$description'].$t)),
        options: (function(){
          var i$, to$, results$ = [];
          for (i$ = 1, to$ = optionCount; i$ <= to$; ++i$) {
            i = i$;
            results$.push({
              name: it["gsx$opt" + i].$t,
              next: parseInt(it["gsx$next" + i].$t) - 2
            });
          }
          return results$;
        }()).filter(function(it){
          return it.name;
        })
      });
    });
  });
}));
function import$(obj, src){
  var own = {}.hasOwnProperty;
  for (var key in src) if (own.call(src, key)) obj[key] = src[key];
  return obj;
}